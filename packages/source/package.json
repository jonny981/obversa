{
  "name": "@obversa/source",
  "version": "0.1.0",
  "private": true,
  "description": "The Pierre review Surface: open a git diff for inline review and return the annotations. Runs on an injected surface port; the composition root binds @obversa/surfacer.",
  "type": "module",
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
