{
  "name": "@obversa/source",
  "version": "0.1.0",
  "private": true,
  "description": "The review surface: open a git diff for inline review and return the annotations. Runs on an injected surface port; the composition root binds @obversa/surfacer.",
  "type": "module",
  "exports": {
    ".": "./src/index.mjs",
    "./testing": "./src/testing.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "acorn": "8.18.0",
    "shiki": "4.4.3"
  }
}
