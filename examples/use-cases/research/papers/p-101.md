# p-101: Duty-cycled sampling for soil moisture sensors (fictional)

Abstract. We sample a capacitive soil moisture sensor on a schedule set by
the previous day's variance instead of a fixed interval. On a bench
fixture the schedule cut sensor wake time by about a third while keeping
the reconstructed curve within the sensor's own noise band. Limits: one
sensor model, indoor fixture, no field trial.
