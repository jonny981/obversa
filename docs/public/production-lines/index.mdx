---
title: "Workflow bank"
description: "Copy complete runtime programs that use only public package exports."
---

A process is a complete program. It composes jobs, graph forms,
policies, and adapters from public package exports.

Each example states its inputs, outputs, permissions, limits, and evidence.
Each example also states its failure and safe-retry behavior.

The first example is [Offline review and revision](/production-lines/offline-review).
It shows a review sending work back once, then passing the corrected result.
It finishes without a model or network service.
